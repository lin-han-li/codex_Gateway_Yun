{
  "name": "codex-gateway-server",
  "version": "1.1.1",
  "private": true,
  "description": "Server deployment bundle for Codex Gateway",
  "type": "module",
  "packageManager": "bun@1.2.23",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "check": "tsc --noEmit",
    "generate:secrets": "bun scripts/generate-secrets.ts"
  },
  "dependencies": {
    "hono": "^4.8.3",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/bun": "^1.2.23",
    "typescript": "^5.9.2"
  }
}
